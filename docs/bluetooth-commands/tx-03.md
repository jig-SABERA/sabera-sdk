---
title: "0x03 通知"
parent: Bluetooth コマンドリスト
nav_order: 2
---

# 0x03 通知

コマンド送信。

| ヘッダ | コマンドID | Type | Length | Value |
|---|---|---|---|---|
| `01` | `03` | `80` | LE(ペイロード長) | 内側TLV |
| 1 byte | 1 byte | 1 byte | 2 byte | 可変長 |

`LE(n)` は整数nを2 byteのリトルエンディアンで表した値。Nは本文または圧縮データのbyte数。
[共通のパケット形式](../bluetooth-commands.md#通常パケットの形式)を参照。

| Type（1 byte） | Length（2 byte、LE） | Value |
|---|---|---|
| `01` | アプリ名の長さ | UTF-8のアプリ名 |
| `02` | 件名の長さ | UTF-8の件名 |
| `03` | `08 00` | Unix時刻（ミリ秒）、8 byte、BE |
| `04` | 2 + 本文の長さ | 分割マーカー（2 byte）→ UTF-8本文 |
| `05` | `02 00` | 件数（1 byte、0〜255）→ `00` |

通知は `01` → `02` → `03` → `04` のTLVを連結して送る。件数は `05` のTLV単独で送る。

本文とアプリ名・件名・日時のTLVの合計が220 byte以下なら、本文のマーカーは `5A 6B`。
分割時の本文は先頭100 byte、以降220 byteまで。文字境界で分割し、先頭に
`01`・`02`・`03`・`04`、以降に `04` のTLVを送る。マーカーは先頭 `5A 5A`、途中 `7C 7C`、末尾 `6B 6B`。

## 関連

- [Bluetooth コマンドリスト](../bluetooth-commands.md)
